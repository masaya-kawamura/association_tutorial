<hr>
<p><span>email: </span><%= @user.email %></p>

<h2><%= @user.email %>の投稿</h2>
<% @tweets.each do |tweet| %>
  <hr>
  <p><span>ツイート内容：</span><%= link_to tweet.body, tweet_path(tweet.id) %></p>
<% end %>

<br/>
<h2><%= @user.email %>がいいねした投稿一覧</h2>

<% @favorite_tweets.each do |tweet| %>
 <hr>
 <p><span>ファボツイート内容：</span><%= link_to tweet.body, tweet_path(tweet.id) %></p>
<% end %>

<br/>
<h2><%= @user.email %>がコメントした投稿</h2>
<% @comment_tweets.each do |tweet| %>
  <hr>
  <p><span>コメントした投稿内容：</span><%= link_to tweet.body, tweet_path(tweet.id) %></p>
<% end %>